# @yabbadabbadev/pepito

Network test utilities for Vitest browser mode: mount your app on a real URL
with mocked traffic, then assert on the network like you assert on the DOM.

**Repo status: migration in progress.** The library is being extracted here
from its evaluation repo, with full English translation. See
`docs/superpowers/specs/2026-08-17-standalone-repo-design.md` and the plan
next to it. The real README arrives with the migration.
