# CHANGELOG.md is generated by release-please, not hand-written: its output
# never matches Prettier's markdown bullet style, so holding it to a
# hand-formatting gate would fail every release for a cosmetic reason.
CHANGELOG.md
